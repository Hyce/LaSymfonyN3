{% extends 'DevschoolAdminBundle::layout.html.twig' %}

    {% block body %}
        <div class="container">
            <a href="{{ path('admin_realisateur_ajout') }}">
                <button type="button" class="btn btn-default pull-right">
                    <span class="glyphicon glyphicon-plus" aria-hidden="true">Ajouter</span>
                </button>
            </a>
            <h1>Liste des réalisateurs :</h1>
                {% for realisateur in realisateurs %}
                    <div class="thumbnail">
                        <h4><a href="{{ path('admin_realisateur_modif', {'id': realisateur.id}) }}">{{ realisateur.nom }} <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a></h4>
                        <a href="{{ path('admin_realisateur_supprimer', {'id': realisateur.id}) }}">Supprimer <span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a>
                    </div>
                {% endfor %}
        </div>


    {% endblock %}